{
  "id": "input",
  "name": "Input",
  "totalProps": 6,
  "packageName": "@rarui-react/input",
  "version": "1.1.0",
  "docLink": "https://rarui.com.br/documentation/src-components/input",
  "props": [
    {
      "description": "Componet to be displayed on start input.",
      "type": "React.ReactNode",
      "title": "leadingStart",
      "required": false
    },
    {
      "description": "Componet to be displayed on end input.",
      "type": "React.ReactNode",
      "title": "leadingEnd",
      "required": false
    },
    {
      "description": "Determines the visual style of the input, affecting its border",
      "enum": ["error", "success"],
      "type": "string",
      "title": "appearance",
      "required": false
    },
    {
      "type": "boolean",
      "title": "border",
      "required": false
    },
    {
      "description": "Places a divider between the input and the leading components",
      "type": "boolean",
      "title": "divider",
      "required": false
    },
    {
      "description": "Specifies the size of the badge, controlling its dimensions.",
      "enum": ["large", "medium", "small"],
      "type": "string",
      "title": "size",
      "required": false
    }
  ],
  "subComponents": [
    {
      "name": "Input.Leading",
      "totalProps": 5,
      "props": [
        {
          "description": "The content of the Leading.",
          "type": "React.ReactNode",
          "title": "children",
          "required": true
        },
        {
          "description": "Sent Leading display position.",
          "enum": ["end", "start"],
          "type": "string",
          "title": "position",
          "required": true
        },
        {
          "description": "Places a divider between the input and the leading components",
          "type": "boolean",
          "title": "divider",
          "required": false
        },
        {
          "description": "Specifies the size of the badge, controlling its dimensions.",
          "enum": ["large", "medium", "small"],
          "type": "string",
          "title": "size",
          "required": false
        },
        {
          "description": "Determines the visual style of the input, affecting its border",
          "enum": ["error", "success"],
          "type": "string",
          "title": "appearance",
          "required": false
        }
      ]
    },
    {
      "name": "Input.Password",
      "totalProps": 4,
      "props": [
        {
          "type": "boolean",
          "title": "border",
          "required": false
        },
        {
          "description": "Places a divider between the input and the leading components",
          "type": "boolean",
          "title": "divider",
          "required": false
        },
        {
          "description": "Specifies the size of the badge, controlling its dimensions.",
          "enum": ["large", "medium", "small"],
          "type": "string",
          "title": "size",
          "required": false
        },
        {
          "description": "Determines the visual style of the input, affecting its border",
          "enum": ["error", "success"],
          "type": "string",
          "title": "appearance",
          "required": false
        }
      ]
    },
    {
      "name": "Input.Search",
      "totalProps": 4,
      "props": [
        {
          "type": "boolean",
          "title": "border",
          "required": false
        },
        {
          "description": "Places a divider between the input and the leading components",
          "type": "boolean",
          "title": "divider",
          "required": false
        },
        {
          "description": "Specifies the size of the badge, controlling its dimensions.",
          "enum": ["large", "medium", "small"],
          "type": "string",
          "title": "size",
          "required": false
        },
        {
          "description": "Determines the visual style of the input, affecting its border",
          "enum": ["error", "success"],
          "type": "string",
          "title": "appearance",
          "required": false
        }
      ]
    }
  ]
}
